name: flutter_counter_bloc_example
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.0.2
  flutter_bloc: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  bloc_test: ^9.0.2
  mocktail: ^0.2.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
